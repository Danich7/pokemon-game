const About = () => {
    const handlerClick = () => {
        
    };

    return (
        <>
            <h1>This is About Page, Hello!</h1>
            <button onClick={handlerClick}>
                Change Theme
            </button>
        </>
    );
};

export default About;
